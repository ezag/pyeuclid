language: python
python:
  - "2.6"
  - "2.7"
  # Coverage.py doesn't support Python 3.2 and so do we
  - "3.3"
  - "3.4"
  - "3.5"
install:
  - pip install -e .
  - pip install python-coveralls
script:
  - make COVERAGE="coverage run --omit=runtests.py" doctest
after_success:
  - coveralls
